# AI-VCS bootstrap study

This directory contains the study protocol and evidence boundary for
`experiments/002-aivcs-bootstrap`, which is the selected AI-native VCS project
stored as a Git submodule.

Keep path-specific artifacts separate:

```text
experiments/002-aivcs-bootstrap-study/
  EXPERIMENT_NOTES.md
  path-a-domain-record/
  path-b-projections/
  path-c-change-proposal/
experiments/002-aivcs-bootstrap/  # pinned external repository
```

Do not place study conclusions inside the submodule. Update the selected
project only through its own repository and record the resulting commit here.
