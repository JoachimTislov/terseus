# RefViz bootstrap study

This directory contains the isolated benchmark for the pinned RefViz project.
The common scoring scheme is in `EXPERIMENT_NOTES.md`; each path must keep its
own artifacts and conclusion:

```text
path-a-domain-record/
path-b-projections/
path-c-change-proposal/
```

The submodule at `../003-refviz-bootstrap` is external and must only be
changed in its own repository.
